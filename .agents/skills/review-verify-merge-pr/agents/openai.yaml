interface:
  display_name: "Review, Verify, and Merge PR"
  short_description: "Safely gate pull request reviews and merges"
  default_prompt: "Use $review-verify-merge-pr with gh to review the exact pull-request head and carry an autonomous implementation through guarded readiness and merge unless an explicit opt-out applies."
