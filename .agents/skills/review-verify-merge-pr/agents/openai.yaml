interface:
  display_name: "Review, Verify, and Merge PR"
  short_description: "Safely gate pull request reviews and merges"
  default_prompt: "Use $review-verify-merge-pr with gh to review the current pull request, verify its exact head, and merge it only if explicitly authorized and clean."
